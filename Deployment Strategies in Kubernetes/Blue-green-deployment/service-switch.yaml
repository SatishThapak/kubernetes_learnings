# service-switch.yaml
apiVersion: v1
kind: Service
metadata:
  name: demo-service
spec:
  selector:
    app: demo
    version: green   # switched from blue → green
  ports:
  - port: 80
    targetPort: 5000
